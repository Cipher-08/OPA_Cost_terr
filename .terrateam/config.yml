hooks:
  plan:
    post:
      - type: run
        cmd: ['conftest', 'test', '--policy', '$TERRATEAM_ROOT/policies/gcp/compute-instance.rego', '--input', 'json', '$TERRATEAM_ROOT/path/to/dummy-input.json']
        capture_output: true

  all:
    pre:
      - type: oidc
        provider: aws
        role_arn: "arn:aws:iam::017777978390:role/terrateam"           
